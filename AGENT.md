# Agent context

Il contesto operativo per assistenti AI su questo repository vive in **`AGENTS.md`** (nome al plurale, convenzione soli92).

Apri [`AGENTS.md`](./AGENTS.md) per architettura, API, RAG (ingest REST + **ingest-stream** SSE), **pagina `/admin`** (cookie session), test (**Vitest** incl. `lib/admin-session.test.ts`) e variabili d’ambiente.
